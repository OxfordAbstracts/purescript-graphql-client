{
  "name": "4-mutation",
  "version": "1.0.0",
  "description": "Handle mutations in with code gen.",
  "main": "server.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "npm i && spago install && node ./test.mjs",
    "build": "spago build",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@apollo/client": "3.6",
    "express": "4.17.1",
    "express-graphql": "0.12.0",
    "graphql": "^16.5.0",
    "graphql-ws": "^5.9.1",
    "rimraf": "3.0.2"
  },
  "devDependencies": {
    "exec-sh": "^0.4.0"
  }
}
